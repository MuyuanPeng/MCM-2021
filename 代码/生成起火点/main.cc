#include<stdio.h>

#define PEOPLE 20

int main()
{
	// 1.在半径为10km的范围内抽样，得到起火点
	// 2.对于每个起火点，抽样得到起火大小
	// 3.根据起火大小，抽样得到消防人员的位置
	setfire_start();
	setfire_size();
	setfire_firemen();
	setfire_write_file();
	return 0;
}

void setfire_start()
{
	;
}
void setfire_size()
{
	;
}
void setfire_firemen()
{
	;
}
void setfire_write_file()
{
	;
}